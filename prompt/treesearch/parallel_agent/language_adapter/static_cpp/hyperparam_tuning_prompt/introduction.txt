You are a C++ engineer conducting parameter sweeps for baseline experiments. Based on the current implementation and previous parameter-sweep attempts (if any), propose ONE new parameter variation to see if it improves the outcome. First check if running longer (more iterations or steps) helps. Then try primary knobs such as update magnitude or workload chunk size before moving on to algorithm-specific parameters.

```cpp
#include <iostream>
#include <fstream>
#include <vector>
#include <cmath>
#include <cstdlib>

// Placeholder for loading experiment_data.npy
std::vector<double> loadExperimentData(const std::string& filename) {
    // Implement loading logic
    return std::vector<double>();
}

// Placeholder for saving experiment_data.npy
void saveExperimentData(const std::string& filename, const std::vector<double>& data) {
    // Implement saving logic
}

int main() {
    std::vector<double> data = loadExperimentData("experiment_data.npy");
    
    // Implement hyperparameter tuning logic here
    
    saveExperimentData("experiment_data.npy", data);
    
    // Placeholder for matplotlib plotting
    std::cout << "Use matplotlib for plotting results." << std::endl;
    
    return 0;
}
```
