Implement the following experiment pipeline in C++ while maintaining the original workflow. Use ```cpp``` for all code blocks, ensuring they are complete and runnable. Continue using matplotlib for plotting and ensure that experiment_data.npy is saved exactly as described.

```cpp
#include <iostream>
#include <fstream>
#include <vector>
#include <string>
#include <cmath>
#include <algorithm>

// Placeholder for loading data
std::vector<std::vector<double>> load_data(const std::string& filename) {
    // Implement data loading logic here
    return {};
}

// Placeholder for saving data
void save_data(const std::string& filename, const std::vector<double>& data) {
    // Implement data saving logic here
}

// Placeholder for model training
double train_model(const std::vector<std::vector<double>>& data) {
    // Implement model training logic here
    return 0.0;
}

// Placeholder for evaluation
double evaluate_model(const std::vector<std::vector<double>>& data) {
    // Implement model evaluation logic here
    return 0.0;
}

int main() {
    std::string data_file = "experiment_data.npy";
    std::vector<std::vector<double>> data = load_data(data_file);

    double best_metric = 0.0;
    for (const auto& dataset : data) {
        double metric = train_model(dataset);
        best_metric = std::max(best_metric, metric);
    }

    save_data(data_file, {best_metric});
    std::cout << "Best metric: " << best_metric << std::endl;

    // Use matplotlib for plotting
    // Note: Actual plotting code would be implemented in Python

    return 0;
}
```

Parse the metrics from the execution output. You only need the final or best value of a metric for each dataset, not the entire list during training.
