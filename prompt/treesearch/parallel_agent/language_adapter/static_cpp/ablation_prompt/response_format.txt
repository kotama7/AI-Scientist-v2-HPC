ABLATION NAME: <ablation name>  
ABLATION DESCRIPTION: <description>  

```cpp
#include <iostream>
#include <fstream>
#include <vector>
#include <cmath>
#include <string>

// Function to load data from experiment_data.npy
std::vector<double> loadData(const std::string& filename) {
    std::vector<double> data;
    // Implement loading logic here
    return data;
}

// Function to perform the experiment
void performExperiment(const std::vector<double>& data) {
    // Implement experiment logic here
}

// Function to save results
void saveResults(const std::string& filename, const std::vector<double>& results) {
    std::ofstream file(filename);
    if (file.is_open()) {
        for (const auto& result : results) {
            file << result << "\n";
        }
        file.close();
    }
}

int main() {
    std::string dataFile = "experiment_data.npy";
    std::string resultFile = "results.txt";

    std::vector<double> data = loadData(dataFile);
    performExperiment(data);
    std::vector<double> results; // Assume results are populated in performExperiment
    saveResults(resultFile, results);

    // Note: Use matplotlib for plotting
    std::cout << "Experiment completed. Results saved to " << resultFile << std::endl;
    return 0;
}
```

Your response should start with 'ABLATION NAME: <ablation name>' on the first line to represent the name of the ablation. The second line should start with 'ABLATION DESCRIPTION: <description>', a brief description of what component is being ablated and why (3-5 sentences),
